define(['shared/config'], function (config) {

  config.DobjectIdName = 'Dobject';
  config.tagSeparator = '-';
  config.pageNotFound = "Sorry our minions could not find the requested page...";
 
  return config;
});
